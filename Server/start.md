# Server.start( )

Starts your server instance.

```javascript
const { Server } = require( 'firecomm' );
const server = new Server();
server.start()
```

### parameters `none`

### returns `undefined`

### example

```javascript
const { Server } = require( 'firecomm' );
const server = new Server();
server.start()
```